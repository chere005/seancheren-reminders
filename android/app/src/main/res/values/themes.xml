<resources>
    <!-- No AppCompat: the app is a ComponentActivity hosting Compose. Dark window so
         there's no white flash before the first frame draws. -->
    <style name="Theme.SuiteApp" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">#111111</item>
        <item name="android:statusBarColor">#111111</item>
        <item name="android:navigationBarColor">#111111</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
