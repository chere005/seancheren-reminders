# Android / Gradle build artifacts and machine-local state. The wrapper, the
# gradle files, sources and the version catalog ARE committed.
.gradle/
build/
.kotlin/
local.properties
captures/
.cxx/
*.iml
.idea/
.DS_Store
